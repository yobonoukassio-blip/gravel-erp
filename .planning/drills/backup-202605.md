# Backup & PITR Drill — 202605 (BASELINE — not yet executed)

## Status

- Workflow: `.github/workflows/backup-drill.yml`
- First scheduled execution: **2026-06-01 02:00 UTC** (monthly cron `0 2 1 * *`)
- Manual trigger available now via `workflow_dispatch` once secrets are configured

This is the **baseline shell** committed alongside the workflow so the first cron run amends
an existing artifact (clean diff for audit), rather than appearing out of nowhere from `null`.

---

## Pre-execution checklist

- [ ] `DRILL_AWS_ACCESS_KEY_ID` / `DRILL_AWS_SECRET_ACCESS_KEY` configured in GitHub Actions
      secrets (read-only IAM on `s3://gravel-prod-backups`).
- [ ] pgBackRest stanza `gravel-prod` confirmed archiving to `s3://gravel-prod-backups`
      with `archive_command` healthy in production Postgres.
- [ ] `s3://gravel-prod-backups/schema/latest.sql` reference dump exists
      (must be auto-updated by CI on every migration — see `deploy.yml` post-migrate step).
- [ ] Runbook `.planning/runbooks/backup-restore-drill.md` reviewed by on-call rota.
- [ ] First dry-run executed via `workflow_dispatch` (do not wait for cron) and result reviewed.

---

## Result (to be populated after first run)

| Field | Value |
|-------|-------|
| Date executed (UTC) | — |
| Trigger | — |
| RTO measured | — s |
| RPO measured | — s |
| Schema diff | — lines |
| Public table count | — |
| Pass/Fail | — |
| Run URL | — |

---

## Notes

First drill run is part of the **v1.1 cutover pre-flight checklist** (HRD-MVP-08 §3, T-7d).
Subsequent runs are autonomous on the monthly cron; humans only intervene on FAIL.

If the first dry-run fails for any reason, the failure mode and remediation map to
`.planning/runbooks/backup-restore-drill.md` §6.

---

## References

- Runbook: `.planning/runbooks/backup-restore-drill.md`
- Workflow: `.github/workflows/backup-drill.yml`
- Scripts: `scripts/dr/restore-pgbackrest.sh`, `scripts/dr/schema-diff.sh`
- Decisions: D-05 (monthly cron + PITR T-1h + schema-diff), D-06 (RTO=1h / RPO=5min), D-07 (artifact commit for audit)
