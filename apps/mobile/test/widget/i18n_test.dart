// REQ: FND-09 — FR ↔ EN per-user, persisted, propagated to web + mobile.
// Implementing plan: W1-P04 (i18n switcher).
//
// Wave 0 RED.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FR ↔ EN switch updates UI', (tester) async {
    fail('NOT IMPLEMENTED — plan W1-P04 (i18n)');
  });

  testWidgets('default locale follows preferred_locale claim, falls back to fr-CI', (tester) async {
    fail('NOT IMPLEMENTED — plan W1-P04');
  });
}
