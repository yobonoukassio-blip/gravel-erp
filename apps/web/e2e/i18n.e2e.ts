/**
 * REQ: FND-09 — FR ↔ EN per-user, persisted, propagated to web + mobile.
 * Implementing plan: W1-P04 (identity + i18n switcher).
 *
 * Wave 0 RED.
 */
import { test } from '@playwright/test';

test('user toggles FR ↔ EN; preference persists and applies to mobile and web', async () => {
  throw new Error('NOT IMPLEMENTED — plan W1-P04 (i18n)');
});

test('default locale is fr-CI when user has no preferred_locale claim', async () => {
  throw new Error('NOT IMPLEMENTED — plan W1-P04');
});
