/**
 * REQ: FND-04 — admin tenant crée site (timezone, devise, GPS, permis).
 * Implementing plan: W2-P05 (master-data UI).
 *
 * Wave 0 RED.
 */
import { test } from '@playwright/test';

test('admin tenant creates Site with iana_timezone, functional_currency, GPS, permit', async () => {
  throw new Error('NOT IMPLEMENTED — plan W2-P05 (master-data E2E)');
});

test('archived site is hidden from operational pickers', async () => {
  throw new Error('NOT IMPLEMENTED — plan W2-P05');
});
