import { Routes } from '@angular/router';

/**
 * Application routes. Phase 1 routes (sites, zones, permits, users, activity-log)
 * are added by plans W1-P04 and W2-P05.
 */
export const routes: Routes = [];
