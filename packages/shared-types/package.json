{
  "name": "@gravel/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types between @gravel/api and @gravel/web (JWT claims, conflict policies, money, etc.)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p .",
    "test:quick": "echo \"@gravel/shared-types: no tests yet (Phase 1 stubs only)\"",
    "test:ci": "echo \"@gravel/shared-types: no tests yet (Phase 1 stubs only)\"",
    "lint": "echo \"@gravel/shared-types: no lint yet\""
  },
  "devDependencies": {
    "typescript": "~5.9.0"
  }
}
